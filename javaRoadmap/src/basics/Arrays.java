package basics;

public class Arrays {
    public void arrays(){
            // 1. Declaring an array (no values yet)
            int[] numbers;

            // 2. Creating / initializing the array (size = 3)
            numbers = new int[3];

            // 3. Assigning values
            numbers[0] = 10;
            numbers[1] = 20;
            numbers[2] = 30;

            // 4. Printing values
            System.out.println(numbers[0]);
            System.out.println(numbers[1]);
            System.out.println(numbers[2]);

        }
    }

