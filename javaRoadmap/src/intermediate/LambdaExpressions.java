package intermediate;

@FunctionalInterface// is an annotation in Java that tells the compiler: “This interface must have exactly ONE abstract method.”
public interface LambdaExpressions {
    void run();
}